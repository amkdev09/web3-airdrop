const FONT_SIZE = {
  DISPLAY: "clamp(1.75rem, 4vw, 4rem)",
  HEADER: "clamp(1.25rem, 2.2vw, 2rem)",
  TITLE: "clamp(0.95rem, 1.4vw, 1.25rem)",
  BODY: "clamp(0.875rem, 1vw, 0.95rem)",
  BODY2: "clamp(0.75rem, 1vw, 0.75rem)",
  CAPTION: "clamp(0.65rem, 0.9vw, 0.85rem)",
};

const ICON_SIZE = {
  XL: 48,
  LG: 32,
  MD: 24,
  SM: 20,
  XS: 16,
};

const ELEVATION = {
  HIGH: 8,
  MEDIUM: 4,
  LOW: 2,
};

const SPACING = {
  XL: 2.5,
  LG: 2,
  MD: 1.5,
  SM: 1,
  XS: 0.5,
  XXS: 0.25,
};

const BORDER_RADIUS = {
  XL: 20,
  NL: 16,
  LG: 12,
  MD: 8,
  SM: 4,
  XS: 2,
};

export { FONT_SIZE, ICON_SIZE, ELEVATION, SPACING, BORDER_RADIUS };
